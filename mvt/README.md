# Marketing Validation Team

Standalone MVT subproject for Loki Mode.

Run from this folder with Node 24:

```bash
node --experimental-strip-types ./src/cli.ts --help
```

Runtime state is created under `mvt/.mvt/`.
